services:
  - type: web
    name: jiaicb
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn server:app
    envVars:
      - key: JTAICB_API_KEY
        sync: false
